# Compiled binary (matches the module name, built by `go build`)
/agent

# Go build/test artifacts
*.exe
*.test
*.out
coverage.*

# Local env / secrets — never commit these
.env
.env.*
*.pem
*.key
secret-file.txt

# macOS
.DS_Store
